import 'package:flutter/material.dart';
// import 'custom_app_bar.dart';

class ShopScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: NotHomeAppBar(
      //   title: 'Shop',
      //   backgroundColor: Colors.green,
      // ),
      body: Center(
        child: Text('쇼핑'),
      ),
    );
  }
}